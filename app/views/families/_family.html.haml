.family-listing{class: cycle("even", "odd"), data: {family: family.id}}
  = [family.parent_1_name, family.parent_2_name].join(', ')
  - family.students.each do |student|
    %br
    = "#{student.name}, #{student.email}, #{student.phone_number}"
  
    